spring:
  application:
    name: shutdown-tracker-project-worker
  flyway:
    enabled: false

server:
  port: ${PORT:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info
