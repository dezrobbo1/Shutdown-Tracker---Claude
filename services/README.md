# Services

Backend services workspace.

- [API](api) contains a placeholder Spring Boot API shell.
- [Project Worker](project-worker) contains a Spring Boot worker shell with a local-only MPXJ import summary spike.

Both services include `local` profile PostgreSQL/Flyway runtime wiring. No domain logic, persistence, export generation, scheduler logic, or frontend code exists here yet.
