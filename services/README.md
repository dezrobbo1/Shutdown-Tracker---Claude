# Services

Backend services workspace.

- [API](api) contains a placeholder Spring Boot API shell.
- [Project Worker](project-worker) contains a placeholder Spring Boot worker shell.

Both services include `local` profile PostgreSQL/Flyway runtime wiring. No domain logic, import/export processing, scheduler logic, or frontend code exists here yet.
